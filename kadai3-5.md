## 自動販売機（データ構造と各種処理）のミニレポート
### Q5-1. 自動販売機の商品データついて説明せよ。
* データ構造（各項目とその説明）
* 各商品の情報（名前・価格・在庫など）を1つのオブジェクトにまとめることで、管理性が高まる
* 連想配列の配列として定義するメリット
* 将来的に メーカー や 賞味期限 などの項目を追加する場合でも、個々のオブジェクトにプロパティを追加するだけで済む。
### Q5-2. showItemListの処理内容について説明せよ。
* 配列の繰り返し処理
* items.length は商品数（配列の長さ）を取得。i を使って items[i] で配列の各要素（1つの商品）を順に取り出している。
* 連想配列の参照方法
* item.id や item.name のようにドット記法で値を取得

### Q5-3. buyItemの処理内容について説明せよ。
* 商品購入の可否判定
* 指定された商品の在庫数 (stock) が 0 より多い場合のみ購入可能。
*　在庫が0の場合は「在庫がありません。」とメッセージを表示し、購入処理を中止。
* 商品在庫を減らす処理
* 商品が購入されたら、在庫数を1減らします。
*この操作により、次の購入時には在庫が反映された状態になります。
* 商品番号のエラー処理
* ユーザーが指定した no が商品一覧の範囲外（例：1未満や配列の長さより大きい）であれば、無効な番号としてエラーメッセージを表示。return で処理を中断し、それ以上の処理が行われないようにします。
### Q5-4. プログラムの考察
* データ構造について
* このプログラムでは、自動販売機の商品データを「連想配列（オブジェクト）の配列」として管理しています。
* 商品一覧表示と購入処理を関数化したメリット
* showItemList() と buyItem(no) を何度でも呼び出せるため、コードを何度も書き直す必要がない。
### Q5-5. 感想
* 今回の課題で苦労したこと
* 課題が多くて時間がかかった。
* 忘れていたものがあったので調べるのが大変だった。
* 演習を通して理解できたこと
* showItemList() と buyItem(no) は便利な機能だと分かった
* この自動販売機プログラムの追加機能や課題など
* 投入金額の管理機能
